apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "eks-auto-pod-id-assoc.fullname" . }}
rules:
- apiGroups:
  - ""
  resources:
  - serviceaccounts
  verbs:
  - list
